import styled from "styled-components";

const Home = (props) => {
  return (
    <Container>
      <Wrap>
        <h1> Hello </h1>
      </Wrap>
    </Container>
  );
};

const Container = styled.div`
  margin-top: 100px;
`;

const Wrap = styled.div``;

export default Home;
